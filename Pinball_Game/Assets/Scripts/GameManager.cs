using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManager : MonoBehaviour
{

    public static bool ballFlag;

    // Start is called before the first frame update
    void Start()
    {
        ballFlag = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
